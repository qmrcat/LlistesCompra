# Archivo de ejemplo de configuración de variables de entorno
# Copia este archivo como .env y modifica según tu configuración

# Puerto para el servidor
PORT=3000

# Clave secreta para JWT
JWT_SECRET=comprajunts_secret
JWT_EXPIRATION=7d

# Configuración de la base de datos
# Estos valores solo se usan si se cambia a MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=comprajunts
DB_USER=root
DB_PASSWORD=

# URL de la aplicación (para enlaces en emails)
APP_URL=http://localhost:3000

# Configuración SMTP para el envío de correos
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_correo@gmail.com
SMTP_PASS=tu_contraseña_o_app_password
EMAIL_FROM=CompraJunts <notificaciones@comprajunts.com>